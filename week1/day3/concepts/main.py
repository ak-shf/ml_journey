# when we have the file in the same dir we can directky import from the dir  
from sum import sum

# from utils.division import div
from utils import sub

from utils import div

print(sub.sub(2,-3))
print(div.div(6,3))

print(sum(1,3))